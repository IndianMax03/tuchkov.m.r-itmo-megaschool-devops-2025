apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "helm-check-action-sample.fullname" . }}-test-connection"
  labels:
    {{- include "helm-check-action-sample.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "helm-check-action-sample.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
